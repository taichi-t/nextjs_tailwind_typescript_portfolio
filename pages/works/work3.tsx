import WorkPagesLayout from '@/components/WorkPagesLayout/WorkPagesLayout';
import Config from '@/next.config';

export default function Work1() {
  return <WorkPagesLayout {...Config.work3Config} />;
}
